# Setup
## Start supabase
- Create new .env file in supabase directory from .env.old
- Run `make setup-supabase`
- Run `make start-supabase`

## Start frontend
- Create new .env file from .env.example
- Use credential from .env Supabase local
- Run `make build-fe`
- Run `make start-fe`